# Development environment for Vite with hot reloading
FROM node:18-alpine

WORKDIR /app

# Copy package.json and install dependencies
COPY package*.json ./
RUN npm install

# Source code is expected to be mounted as a volume when running docker-compose or docker run

# Set environment variables if needed
ENV NODE_ENV=development

# Expose the default Vite dev server port
EXPOSE 5173

# Start Vite dev server with host 0.0.0.0 so it is accessible from outside container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
