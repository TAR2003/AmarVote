# Development environment for Spring Boot with hot reloading
FROM maven:3.9-eclipse-temurin-21

WORKDIR /app

# Copy only the pom.xml and download dependencies to improve rebuild efficiency
COPY pom.xml .
# Cache Maven dependencies
RUN mvn dependency:go-offline -B

# No need to copy source code as it will be mounted as a volume in docker-compose.yml
# This enables hot reloading with Spring DevTools

# Expose port
EXPOSE 8080

# Start the application in development mode
CMD ["mvn", "spring-boot:run", "-Dspring-boot.run.jvmArguments=-Dspring.devtools.restart.enabled=true"]
